# School Calendar 2026-27 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrate the school calendar to AY 2026-27 with new data schema, category consolidation, CMD+K search, school timings view, fee modal details, and browser push notifications.

**Architecture:** New `data.json` with extended schema (fee_schedule, ptm_schedule, exam_schedule, school_timings, holidays, vacations) feeds updated `realCalendarData.js` which merges all sources into `allCalendarEvents`. Three new components (SearchModal, TimingsView, NotificationSettings) plus a Service Worker handle search and notifications. Categories consolidated from 9 to 7.

**Tech Stack:** React 18, CRA/react-scripts, Tailwind CSS, lucide-react, moment.js, Web Notifications API, Service Worker, localStorage

---

## File Map

| File | Action |
|---|---|
| `package.json` | Upgrade deps, add audit fixes |
| `src/data/data.json` | Replace with 2026-27 extended schema |
| `src/data/realCalendarData.js` | New transforms + category consolidation |
| `src/config/appConfig.js` | Update year, colors for 7 categories |
| `src/App.js` | Remove inline search, add SearchModal/Timings tab/notification bell |
| `src/components/Header.js` | Add notification bell button |
| `src/components/EventModal.js` | Fee section + updated category icons/labels |
| `src/components/Dashboard.js` | Updated category counts (7 categories) |
| `src/components/SectionsView.js` | Updated category list |
| `src/components/MobileAgenda.js` | Updated category colors |
| `src/components/SearchModal.js` | **New** — CMD+K search overlay |
| `src/components/TimingsView.js` | **New** — school timings cards |
| `src/components/NotificationSettings.js` | **New** — notification config panel |
| `src/hooks/useNotifications.js` | **New** — scheduling + prefs hook |
| `public/sw.js` | **New** — Service Worker |

---

## Task 1: Dependency Upgrades & Vulnerability Fixes

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Run safe audit fixes**
```bash
npm audit fix
```
Expected: several vulnerabilities resolved, no breaking changes.

- [ ] **Step 2: Upgrade patchable direct deps**
```bash
npm install react-big-calendar@latest autoprefixer@latest postcss@latest
```

- [ ] **Step 3: Verify app still builds**
```bash
CI=false npm run build 2>&1 | tail -5
```
Expected: `Successfully compiled` (or similar — no errors).

- [ ] **Step 4: Commit**
```bash
git add package.json package-lock.json
git commit -m "chore: upgrade deps and fix audit vulnerabilities"
```

---

## Task 2: New data.json for 2026-27

**Files:**
- Replace: `src/data/data.json`

- [ ] **Step 1: Archive old data**
```bash
cp src/data/data.json src/data/data-2025-26.json
```

- [ ] **Step 2: Write new data.json**

Create `src/data/data.json` with this structure, populated from the diary photos:

```json
{
  "school_timings": [
    {
      "group": "Pre-KG",
      "days": "Monday–Friday",
      "start": "9:30 AM",
      "end": "1:00 PM",
      "periods": [
        { "label": "Reporting Time", "time": "9:30 AM" },
        { "label": "Assembly", "time": "9:35 AM – 9:45 AM" },
        { "label": "1st Period", "time": "9:45 AM – 10:15 AM" },
        { "label": "2nd Period", "time": "10:15 AM – 10:45 AM" },
        { "label": "3rd Period", "time": "10:45 AM – 11:15 AM" },
        { "label": "Snack Break (Dry Snacks Only)", "time": "11:15 AM – 11:40 AM" },
        { "label": "4th Period", "time": "11:40 AM – 12:10 PM" },
        { "label": "5th Period", "time": "12:10 PM – 12:40 PM" },
        { "label": "Circle Time & Class Teacher Time", "time": "12:40 PM – 1:00 PM" },
        { "label": "Dispersal", "time": "1:00 PM" }
      ]
    },
    {
      "group": "LKG & UKG",
      "days": "Monday–Friday",
      "start": "8:45 AM",
      "end": "1:00 PM",
      "periods": [
        { "label": "Reporting Time", "time": "8:45 AM" },
        { "label": "Assembly", "time": "8:50 AM – 9:00 AM" },
        { "label": "1st Period", "time": "9:00 AM – 9:30 AM" },
        { "label": "2nd Period", "time": "9:30 AM – 10:00 AM" },
        { "label": "3rd Period", "time": "10:00 AM – 10:30 AM" },
        { "label": "Snack Break (Dry Snacks Only)", "time": "10:30 AM – 10:50 AM" },
        { "label": "4th Period", "time": "10:50 AM – 11:20 AM" },
        { "label": "5th Period", "time": "11:20 AM – 11:50 AM" },
        { "label": "6th Period", "time": "11:50 AM – 12:20 PM" },
        { "label": "7th Period", "time": "12:20 PM – 12:50 PM" },
        { "label": "Class Teacher Time", "time": "12:50 PM – 1:00 PM" },
        { "label": "Dispersal", "time": "1:00 PM" }
      ]
    },
    {
      "group": "Class I & II",
      "days": "Monday–Friday",
      "start": "8:20 AM",
      "end": "3:10 PM",
      "periods": [
        { "label": "Reporting Time", "time": "8:20 AM" },
        { "label": "Assembly", "time": "8:25 AM – 8:45 AM" },
        { "label": "1st Period", "time": "8:45 AM – 9:25 AM" },
        { "label": "2nd Period", "time": "9:25 AM – 10:05 AM" },
        { "label": "3rd Period", "time": "10:05 AM – 10:45 AM" },
        { "label": "Snack Break", "time": "10:45 AM – 11:05 AM" },
        { "label": "4th Period", "time": "11:05 AM – 11:45 AM" },
        { "label": "5th Period", "time": "11:45 AM – 12:25 PM" },
        { "label": "Lunch", "time": "12:25 PM – 1:00 PM" },
        { "label": "6th Period", "time": "1:00 PM – 1:40 PM" },
        { "label": "7th Period", "time": "1:40 PM – 2:20 PM" },
        { "label": "8th Period", "time": "2:20 PM – 3:00 PM" },
        { "label": "Circle Time", "time": "3:00 PM – 3:10 PM" },
        { "label": "Dispersal", "time": "3:10 PM" }
      ]
    },
    {
      "group": "Class III–XII",
      "days": "Monday–Friday",
      "start": "8:20 AM",
      "end": "3:35 PM",
      "periods": [
        { "label": "Reporting Time", "time": "8:20 AM" },
        { "label": "Assembly Time", "time": "8:20 AM – 8:45 AM" },
        { "label": "1st Period", "time": "8:45 AM – 9:25 AM" },
        { "label": "2nd Periods", "time": "9:25 AM – 10:05 AM" },
        { "label": "3rd Periods", "time": "10:05 AM – 10:45 AM" },
        { "label": "Snack Break", "time": "10:25 AM – 11:00 AM" },
        { "label": "4th Period", "time": "11:00 AM – 11:40 AM" },
        { "label": "5th Period", "time": "11:40 AM – 12:20 PM" },
        { "label": "Lunch", "time": "12:20 PM – 1:00 PM" },
        { "label": "6th Period", "time": "1:00 PM – 1:40 PM" },
        { "label": "7th Period", "time": "1:40 PM – 2:10 PM" },
        { "label": "8th Period", "time": "2:10 PM – 2:50 PM" },
        { "label": "9th Period", "time": "2:50 PM – 3:25 PM" },
        { "label": "Class Teacher Time", "time": "3:25 PM – 3:35 PM" },
        { "label": "Dispersal", "time": "3:35 PM" }
      ]
    },
    {
      "group": "Class III–XII (Saturday)",
      "days": "Saturday",
      "start": "8:20 AM",
      "end": "1:00 PM",
      "periods": [
        { "label": "Reporting Time", "time": "8:20 AM" },
        { "label": "Assembly Time", "time": "8:20 AM – 8:45 AM" },
        { "label": "1st Period", "time": "8:45 AM – 9:25 AM" },
        { "label": "2nd Period", "time": "9:25 AM – 10:05 AM" },
        { "label": "3rd Period", "time": "10:05 AM – 10:45 AM" },
        { "label": "4th Period", "time": "10:45 AM – 11:25 AM" },
        { "label": "5th Period", "time": "11:25 AM – 12:05 PM" },
        { "label": "6th Period", "time": "12:05 PM – 12:45 PM" },
        { "label": "Dispersal", "time": "1:00 PM" }
      ]
    },
    {
      "group": "Office Hours",
      "days": "Monday–Friday",
      "start": "9:00 AM",
      "end": "5:00 PM",
      "periods": []
    },
    {
      "group": "Office Hours (Saturday)",
      "days": "Saturday",
      "start": "9:00 AM",
      "end": "1:00 PM",
      "periods": []
    },
    {
      "group": "After School Activity (ASA)",
      "days": "Monday–Friday",
      "start": "3:45 PM",
      "end": "4:45 PM",
      "periods": []
    }
  ],
  "fee_schedule": [
    { "term": "Term 1", "class_range": "Pre-KG", "amount": 34700, "cutoff_start": "2026-04-01", "cutoff_end": "2026-04-12" },
    { "term": "Term 1", "class_range": "LKG & UKG", "amount": 44700, "cutoff_start": "2026-04-01", "cutoff_end": "2026-04-12" },
    { "term": "Term 1", "class_range": "Class I–II", "amount": 48700, "cutoff_start": "2026-03-02", "cutoff_end": "2026-03-14" },
    { "term": "Term 1", "class_range": "Class III–V", "amount": 48950, "cutoff_start": "2026-03-02", "cutoff_end": "2026-03-14" },
    { "term": "Term 1", "class_range": "Class VI–VIII", "amount": 50950, "cutoff_start": "2026-03-02", "cutoff_end": "2026-03-14" },
    { "term": "Term 1", "class_range": "Class IX–X", "amount": 51000, "cutoff_start": "2026-03-02", "cutoff_end": "2026-03-14" },
    { "term": "Term 1", "class_range": "Class XI–XII", "amount": 51700, "cutoff_start": "2026-03-02", "cutoff_end": "2026-03-14" },
    { "term": "Term 2", "class_range": "Pre-KG", "amount": 34700, "cutoff_start": "2026-10-01", "cutoff_end": "2026-10-12" },
    { "term": "Term 2", "class_range": "LKG & UKG", "amount": 44700, "cutoff_start": "2026-10-01", "cutoff_end": "2026-10-12" },
    { "term": "Term 2", "class_range": "Class I–II", "amount": 48700, "cutoff_start": "2026-10-01", "cutoff_end": "2026-10-12" },
    { "term": "Term 2", "class_range": "Class III–V", "amount": 48950, "cutoff_start": "2026-10-01", "cutoff_end": "2026-10-12" },
    { "term": "Term 2", "class_range": "Class VI–VIII", "amount": 51000, "cutoff_start": "2026-10-01", "cutoff_end": "2026-10-12" },
    { "term": "Term 2", "class_range": "Class IX–X", "amount": 51000, "cutoff_start": "2026-10-01", "cutoff_end": "2026-10-12" },
    { "term": "Term 2", "class_range": "Class XI–XII", "amount": 51700, "cutoff_start": "2026-10-01", "cutoff_end": "2026-10-12" },
    { "term": "Books & Uniforms", "class_range": "Pre-KG", "amount": 11500, "cutoff_start": "2027-02-01", "cutoff_end": "2027-02-12" },
    { "term": "Books & Uniforms", "class_range": "LKG & UKG", "amount": 18700, "cutoff_start": "2027-02-01", "cutoff_end": "2027-02-12" },
    { "term": "Books & Uniforms", "class_range": "Class I–VIII", "amount": 19200, "cutoff_start": "2027-02-01", "cutoff_end": "2027-02-12" },
    { "term": "Books & Uniforms", "class_range": "Class IX–XII", "amount": 19700, "cutoff_start": "2027-02-01", "cutoff_end": "2027-02-12" }
  ],
  "ptm_schedule": [
    { "type": "Parent Interactive Meeting", "term": 1, "class_range": "KG, Classes I–XII", "dates": ["2026-07-31", "2026-08-01"] },
    { "type": "Custodian Meeting", "term": 1, "class_range": "X & XII", "dates": ["2026-09-26"] },
    { "type": "Parent Teacher Meeting", "term": 1, "class_range": "KG, Classes I–XII", "dates": ["2026-10-30", "2026-10-31"] },
    { "type": "Custodian Meeting", "term": 1, "class_range": "X & XII", "dates": ["2026-11-28"] },
    { "type": "Parent Interactive Meeting", "term": 2, "class_range": "KG, Classes I–XII", "dates": ["2026-11-11", "2026-12-12"] },
    { "type": "Parent Teacher Meeting", "term": 2, "class_range": "Classes I–IX, XI", "dates": ["2027-02-26", "2027-02-27"] },
    { "type": "Parent Teacher Meeting", "term": 2, "class_range": "KG", "dates": ["2027-04-15"] }
  ],
  "exam_schedule": [
    { "assessment": "FA/PA1 Unit Test 1", "class_range": "X, XI", "date_start": "2026-04-08", "date_end": "2026-04-17" },
    { "assessment": "Diagnostic Assessment", "class_range": "KG–IX", "date_start": "2026-07-08", "date_end": "2026-07-15" },
    { "assessment": "FA/PA1 Unit Test 1", "class_range": "I–V, VI–IX", "date_start": "2026-07-08", "date_end": "2026-07-15" },
    { "assessment": "FA/PA1 Unit Test 2", "class_range": "VI–IX", "date_start": "2026-07-08", "date_end": "2026-07-15" },
    { "assessment": "FA/PA1 Unit Test 3", "class_range": "XI", "date_start": "2026-07-08", "date_end": "2026-07-15" },
    { "assessment": "Diagnostic Assessment", "class_range": "KG–II", "date_start": "2026-08-17", "date_end": "2026-08-28" },
    { "assessment": "SA1 / Mid Term Exam", "class_range": "KG–II", "date_start": "2026-10-05", "date_end": "2026-10-16" },
    { "assessment": "SA1 / Mid Term Exam", "class_range": "III–XII", "date_start": "2026-10-05", "date_end": "2026-10-16" },
    { "assessment": "Term Holiday Diagnostic Assessment for SA1 & Mid Term Exam", "class_range": "All", "date_start": "2026-09-22", "date_end": "2026-09-22" },
    { "assessment": "Revision Exam 1", "class_range": "X & XII", "date_start": "2026-11-24", "date_end": "2026-12-02" },
    { "assessment": "Revision Exam 2", "class_range": "Classes X & XII", "date_start": "2026-12-07", "date_end": "2026-12-09" },
    { "assessment": "FA/PA Revision Exam 3", "class_range": "Classes IX, XI", "date_start": "2026-12-10", "date_end": "2026-12-12" },
    { "assessment": "CSSC Exam", "class_range": "X & XII", "date_start": "2026-12-25", "date_end": "2027-01-01" },
    { "assessment": "Model Practical Exam", "class_range": "X & XII", "date_start": "2026-12-16", "date_end": "2026-12-22" },
    { "assessment": "Term 2 PBL Commences", "class_range": "I–VIII", "date_start": "2027-01-21", "date_end": "2027-01-21" },
    { "assessment": "Year End Exam", "class_range": "IX & XI", "date_start": "2027-02-11", "date_end": "2027-02-22" },
    { "assessment": "SA2 / Yearly Exam", "class_range": "All", "date_start": "2027-03-31", "date_end": "2027-04-12" },
    { "assessment": "Diagnostic Assessment", "class_range": "KG", "date_start": "2027-03-31", "date_end": "2027-04-07" },
    { "assessment": "Diagnostic Assessment (SA2)", "class_range": "I–VIII", "date_start": "2027-03-11", "date_end": "2027-03-29" }
  ],
  "holidays": [
    { "name": "Bakrid", "date": "2026-06-26" },
    { "name": "Muharram", "date": "2026-07-26" },
    { "name": "Independence Day", "date": "2026-08-15" },
    { "name": "Onam / Milad-un-Nabi", "date": "2026-08-31" },
    { "name": "Art Fest 2026", "date": "2026-09-05" },
    { "name": "Gandhi Jayanthi", "date": "2026-10-02" },
    { "name": "Ayudha Pooja", "date": "2026-10-20" },
    { "name": "Vijaya Dashami", "date": "2026-10-21" },
    { "name": "Diwali", "date": "2026-11-08" },
    { "name": "Christmas", "date": "2026-12-25" },
    { "name": "Thiruvalluvar Day", "date": "2027-01-15" },
    { "name": "Uzhavar Thirunal", "date": "2027-01-16" },
    { "name": "Pongal", "date": "2027-01-14" },
    { "name": "Republic Day", "date": "2027-01-26" },
    { "name": "Good Friday", "date": "2027-03-26" },
    { "name": "Ugadi", "date": "2027-03-19" },
    { "name": "Tamil New Year", "date": "2027-04-14" }
  ],
  "vacations": [
    { "name": "Term 1 Holiday", "start": "2026-11-07", "end": "2026-11-25", "applicable_to": "All" },
    { "name": "Term 2 Holiday", "start": "2027-01-17", "end": "2027-01-25", "applicable_to": "KG & Classes I–IV" },
    { "name": "Term 2 Holiday", "start": "2027-02-13", "end": "2027-03-06", "applicable_to": "Classes I–IV" },
    { "name": "Summer Vacation", "start": "2027-04-17", "end": "2027-06-08", "applicable_to": "KG & Classes I–IV" },
    { "name": "Summer Vacation", "start": "2027-04-09", "end": "2027-06-08", "applicable_to": "Classes V–XII" }
  ],
  "special_assemblies": [
    { "topic": "World Environment Day", "date": "2026-06-05" },
    { "topic": "World Yoga Day", "date": "2026-06-21" },
    { "topic": "World Population Day", "date": "2026-07-11" },
    { "topic": "French Day", "date": "2026-07-14" },
    { "topic": "World Senior Citizens Day", "date": "2026-08-01" },
    { "topic": "Friendship Day", "date": "2026-08-02" },
    { "topic": "Teachers Day", "date": "2026-09-05" },
    { "topic": "Hindi Day", "date": "2026-09-14" },
    { "topic": "International Day of Peace", "date": "2026-09-21" },
    { "topic": "World Elders Day", "date": "2026-10-01" },
    { "topic": "World Music Day", "date": "2026-10-01" },
    { "topic": "World Mental Health Day", "date": "2026-10-10" },
    { "topic": "World Food Day", "date": "2026-10-16" },
    { "topic": "UNO Day", "date": "2026-10-24" },
    { "topic": "Children's Day", "date": "2026-11-14" },
    { "topic": "International Day of Tolerance", "date": "2026-11-16" },
    { "topic": "World Disability Day", "date": "2026-12-03" },
    { "topic": "Human Rights Day", "date": "2026-12-10" },
    { "topic": "National Youth Day", "date": "2027-01-12" },
    { "topic": "Martyrs Day", "date": "2027-01-30" },
    { "topic": "Republic Day", "date": "2027-01-26" },
    { "topic": "National Science Day", "date": "2027-02-28" },
    { "topic": "World Wildlife Day", "date": "2027-03-03" },
    { "topic": "International Women's Day", "date": "2027-03-08" },
    { "topic": "World Water Day", "date": "2027-03-22" },
    { "topic": "World Creativity & Innovation Day", "date": "2027-04-21" },
    { "topic": "International Mother Earth Day", "date": "2027-04-22" }
  ],
  "academic_calendar": [
    {
      "month_year": "June 2026",
      "general_activities": [
        "Parent Orientation Classes KG, I–IX, XI",
        "Class Competition – Choral Recitation English Classes I–V",
        "Class Competition – Poster Making Classes VI–VII",
        "Class Competition – Think Tank – Classes IX–XII",
        "Field Trip Classes IX–XII"
      ],
      "daily_activities": [
        { "day": "Mon", "date": "01", "activity": ["School Reopens Classes III–XII"] },
        { "day": "Tue", "date": "02", "activity": ["School Reopens Classes I–II"] },
        { "day": "Wed", "date": "03", "activity": ["Working Day Classes I–XII"] },
        { "day": "Thu", "date": "04", "activity": ["Working Day Classes I–XII"] },
        { "day": "Fri", "date": "05", "activity": ["Working Day Classes I–XII"] },
        { "day": "Sat", "date": "06", "activity": ["Working Day Classes I–XII"] },
        { "day": "Sun", "date": "07", "activity": [] },
        { "day": "Mon", "date": "08", "activity": ["School Reopens UKG", "Assignment Submission Term 1 Classes II–VIII", "Record & Project Submission Classes IX, X & XII"] },
        { "day": "Tue", "date": "09", "activity": ["School Reopens LKG"] },
        { "day": "Wed", "date": "10", "activity": ["Holiday KG – XII"] },
        { "day": "Thu", "date": "11", "activity": ["Nominations of Students Council Classes VI–XII"] },
        { "day": "Fri", "date": "12", "activity": ["Holiday KG – XII"] },
        { "day": "Sat", "date": "13", "activity": [] },
        { "day": "Mon", "date": "15", "activity": ["School Reopens Pre-KG", "Term 1 PBL Commences Classes III–VIII"] },
        { "day": "Tue", "date": "16", "activity": ["Term 1 PBL Commences Classes III–VIII"] },
        { "day": "Wed", "date": "17", "activity": [] },
        { "day": "Thu", "date": "18", "activity": [] },
        { "day": "Fri", "date": "19", "activity": ["Student Council Elections", "Club Activities Classes III–VIII"] },
        { "day": "Sat", "date": "20", "activity": ["Announcement of Election Results", "Holiday KG & Classes I–II", "Working Day Classes III–XII"] },
        { "day": "Mon", "date": "22", "activity": ["KG – Class Activity: Rainbow Week (Red & Orange)"] },
        { "day": "Tue", "date": "23", "activity": ["KG – Class Activity: Rainbow Week (Blue / Indigo / Violet)"] },
        { "day": "Wed", "date": "24", "activity": ["KG – Class Activity: Rainbow Week (Yellow)"] },
        { "day": "Thu", "date": "25", "activity": ["No Bag Day KG – XII"] },
        { "day": "Fri", "date": "26", "activity": ["Holiday – Muharram"] },
        { "day": "Sat", "date": "27", "activity": [] },
        { "day": "Mon", "date": "29", "activity": [] },
        { "day": "Tue", "date": "30", "activity": ["Investiture Ceremony"] }
      ]
    },
    {
      "month_year": "July 2026",
      "general_activities": [
        "Field Trip Classes III–V",
        "Inter House Competition KG & Classes III–V",
        "Inter House Competition – Set – Classes VI–X",
        "Intramural Athletics Classes VI–X",
        "Photo Class Competition – Cricket – Classes IX–XII"
      ],
      "daily_activities": [
        { "day": "Wed", "date": "01", "activity": ["No Bag Day – Classes I–II"] },
        { "day": "Thu", "date": "02", "activity": [] },
        { "day": "Fri", "date": "03", "activity": ["No Bag Day – Classes I–II"] },
        { "day": "Sat", "date": "04", "activity": ["Holiday – KG & Classes I–II", "Working Day Classes III–XII"] },
        { "day": "Mon", "date": "06", "activity": ["No Bag Day – Classes I–II"] },
        { "day": "Tue", "date": "07", "activity": ["Term 1 UT Commences"] },
        { "day": "Wed", "date": "08", "activity": [] },
        { "day": "Thu", "date": "09", "activity": [] },
        { "day": "Fri", "date": "10", "activity": [] },
        { "day": "Sat", "date": "11", "activity": [] },
        { "day": "Mon", "date": "13", "activity": ["KG – Class Activity: Home Sweet Home"] },
        { "day": "Tue", "date": "14", "activity": [] },
        { "day": "Wed", "date": "15", "activity": [] },
        { "day": "Thu", "date": "16", "activity": [] },
        { "day": "Fri", "date": "17", "activity": ["FA/PA1 Unit Test Ends", "KG – Class Activity: Home Sweet Home"] },
        { "day": "Sat", "date": "18", "activity": [] },
        { "day": "Mon", "date": "20", "activity": [] },
        { "day": "Tue", "date": "21", "activity": ["Health Check-up – KG, I–II", "No Bag Day – Classes I–II"] },
        { "day": "Wed", "date": "22", "activity": ["Health Check-up – KG, III–V"] },
        { "day": "Thu", "date": "23", "activity": ["Health Check-up – KG, VI–VIII", "No Bag Day – Classes I–II"] },
        { "day": "Fri", "date": "24", "activity": ["Result Publication FA/PA / Unit Test"] },
        { "day": "Sat", "date": "25", "activity": ["KG Colouring Competition – Vivid Splendour", "Club Activities Classes III–VIII"] },
        { "day": "Mon", "date": "26", "activity": ["Holiday – Muharram"] },
        { "day": "Tue", "date": "27", "activity": ["LKG – Field Trip"] },
        { "day": "Wed", "date": "28", "activity": ["UKG – Field Trip"] },
        { "day": "Thu", "date": "29", "activity": ["Pre-KG – Field Trip – Joy Ride", "Holiday KG – XII"] },
        { "day": "Fri", "date": "30", "activity": ["Class Activity: Fun Games"] },
        { "day": "Sat", "date": "31", "activity": ["Term 1 – Parent Interactive Meeting (PIM) – KG & Classes I–XII"] }
      ]
    },
    {
      "month_year": "August 2026",
      "general_activities": [
        "Inter House Competition – Group Dance Classes III–V",
        "Inter House Competition Classes VI–X",
        "Poster Maker Classes VI–X",
        "Special Classes IX–XII"
      ],
      "daily_activities": [
        { "day": "Sat", "date": "01", "activity": ["Term 1 – Parent Interactive Meeting (PIM) – KG & Classes I–XII"] },
        { "day": "Mon", "date": "03", "activity": ["Term 1 Intra-Interactive Meeting Begins"] },
        { "day": "Tue", "date": "04", "activity": [] },
        { "day": "Wed", "date": "05", "activity": ["No Bag Day Classes III–VII"] },
        { "day": "Thu", "date": "06", "activity": ["No Bag Day Classes I–II", "No Bag Day Classes III–XII"] },
        { "day": "Fri", "date": "07", "activity": ["No Bag Day Classes I–II, KG", "Working Day Classes I–II, KG"] },
        { "day": "Sat", "date": "08", "activity": ["No Bag Day KG Classes I–III – No Bag Day"] },
        { "day": "Mon", "date": "10", "activity": ["No Bag Day KG Classes I–XII"] },
        { "day": "Tue", "date": "11", "activity": [] },
        { "day": "Wed", "date": "12", "activity": ["No Bag Day KG Classes I–II – No Bag Day", "No Bag Day KG Classes III–XII"] },
        { "day": "Thu", "date": "13", "activity": ["No Bag Day KG Classes I–XII, Working Day Classes III–IX, XI"] },
        { "day": "Fri", "date": "14", "activity": [] },
        { "day": "Sat", "date": "15", "activity": ["Holiday – Independence Day"] },
        { "day": "Mon", "date": "17", "activity": ["FA/PA/UT Commences"] },
        { "day": "Tue", "date": "18", "activity": [] },
        { "day": "Wed", "date": "19", "activity": [] },
        { "day": "Thu", "date": "20", "activity": [] },
        { "day": "Fri", "date": "21", "activity": [] },
        { "day": "Sat", "date": "22", "activity": [] },
        { "day": "Mon", "date": "24", "activity": ["Pre-KG – Class Activity: Talent Show"] },
        { "day": "Tue", "date": "25", "activity": ["UKG Class Competition – Show and Tell – Puppet Talk"] },
        { "day": "Wed", "date": "26", "activity": ["LKG Class Competition – Show and Tell – Puppet Talk"] },
        { "day": "Thu", "date": "27", "activity": ["Pre KG Class Competition – Show and Tell – Puppet Talk"] },
        { "day": "Fri", "date": "28", "activity": ["FA/PA/UT Ends"] },
        { "day": "Sat", "date": "29", "activity": ["Club Activities Classes III–VIII"] },
        { "day": "Mon", "date": "31", "activity": ["Holiday – Onam / Milad-un-Nabi"] }
      ]
    },
    {
      "month_year": "September 2026",
      "general_activities": [
        "Inter House Competition FA/PA/UT",
        "Inter House Competition – Tennis Classes I–X",
        "Inter House Competition – Poster Making Classes VI–X",
        "Result Publication FA/PA/UT",
        "Art Fest 2026 Classes I–X"
      ],
      "daily_activities": [
        { "day": "Tue", "date": "01", "activity": ["Result Publication FA/PA/UT"] },
        { "day": "Wed", "date": "02", "activity": ["KG Class Competition – Storytelling"] },
        { "day": "Thu", "date": "03", "activity": [] },
        { "day": "Fri", "date": "04", "activity": ["No Bag Day – Class I – XII"] },
        { "day": "Sat", "date": "05", "activity": ["Holiday – Krishna Jayanthi"] },
        { "day": "Mon", "date": "07", "activity": [] },
        { "day": "Tue", "date": "08", "activity": ["UKG Class Competition – Rhythm and Rhymes"] },
        { "day": "Wed", "date": "09", "activity": ["LKG Class Competition – Rhythm and Rhymes"] },
        { "day": "Thu", "date": "10", "activity": ["Pre KG Class Competition – Rhythm and Rhymes"] },
        { "day": "Fri", "date": "11", "activity": [] },
        { "day": "Sat", "date": "12", "activity": [] },
        { "day": "Mon", "date": "14", "activity": ["Holiday – KG – XII"] },
        { "day": "Tue", "date": "15", "activity": ["Annual Athletic Meet"] },
        { "day": "Wed", "date": "16", "activity": ["Holiday – Ganesh Chaturthi"] },
        { "day": "Thu", "date": "17", "activity": [] },
        { "day": "Fri", "date": "18", "activity": [] },
        { "day": "Sat", "date": "19", "activity": [] },
        { "day": "Mon", "date": "21", "activity": ["Working Day KG – Classes I–II", "Working Day Classes III–XII"] },
        { "day": "Tue", "date": "22", "activity": ["Revision Commences for Diagnostic Assessment, SA 1 & Mid Term Exam"] },
        { "day": "Wed", "date": "23", "activity": [] },
        { "day": "Thu", "date": "24", "activity": [] },
        { "day": "Fri", "date": "25", "activity": ["Assignment Topic – Term 2 All Subjects Classes I–VIII", "Project Topics – Classes III–VIII", "Club Activities Classes III–VIII"] },
        { "day": "Sat", "date": "26", "activity": ["Custodian Meeting Classes III – VII", "Custodian Meeting Classes X & XII", "Working Day Classes X & XII"] },
        { "day": "Mon", "date": "28", "activity": ["Holiday – KG & Classes I–II", "Working Day Classes I–II"] },
        { "day": "Tue", "date": "29", "activity": ["Clay Modelling Classes III–XII"] },
        { "day": "Wed", "date": "30", "activity": ["Custodian Meeting Classes X & XII"] }
      ]
    },
    {
      "month_year": "October 2026",
      "general_activities": [
        "Intramural Football Classes VI–XI",
        "Inter House Competition Carrom Classes VI–XI",
        "Intramural Athletics Classes VI–XI",
        "Terminal Poster Classes VI–XI"
      ],
      "daily_activities": [
        { "day": "Thu", "date": "01", "activity": ["Holiday – KG, Working Day Classes I–XII"] },
        { "day": "Fri", "date": "02", "activity": ["SA / Mid Term Exam Ends – KG – Class I Ends", "Diagnostics Assessment for Students"] },
        { "day": "Sat", "date": "03", "activity": [] },
        { "day": "Mon", "date": "05", "activity": ["SA / Mid Term Exam Ends – KG – Class I Ends"] },
        { "day": "Tue", "date": "06", "activity": [] },
        { "day": "Wed", "date": "07", "activity": [] },
        { "day": "Thu", "date": "08", "activity": [] },
        { "day": "Fri", "date": "09", "activity": [] },
        { "day": "Sat", "date": "10", "activity": [] },
        { "day": "Mon", "date": "12", "activity": ["Term Holiday Commences for KG – Class I Ends"] },
        { "day": "Tue", "date": "13", "activity": ["Term Holiday Commences for KG – Class I Ends"] },
        { "day": "Wed", "date": "14", "activity": [] },
        { "day": "Thu", "date": "15", "activity": [] },
        { "day": "Fri", "date": "16", "activity": [] },
        { "day": "Sat", "date": "17", "activity": [] },
        { "day": "Mon", "date": "19", "activity": [] },
        { "day": "Tue", "date": "20", "activity": ["Holiday – Ayudha Pooja"] },
        { "day": "Wed", "date": "21", "activity": ["Holiday – Vijaya Dashami"] },
        { "day": "Thu", "date": "22", "activity": ["School Reopens – Term 2 Commences"] },
        { "day": "Fri", "date": "23", "activity": [] },
        { "day": "Sat", "date": "24", "activity": ["KG Class Event – Grandparents Day"] },
        { "day": "Mon", "date": "26", "activity": ["Assignment Submission Classes IX & XI", "Final Project Submission Classes IX, X & XII", "Final Record Submission Classes IX, X & XII"] },
        { "day": "Tue", "date": "27", "activity": ["Result Publication SA / Mid Term Exam 1"] },
        { "day": "Wed", "date": "28", "activity": [] },
        { "day": "Thu", "date": "29", "activity": ["Club Activities Classes III–VIII"] },
        { "day": "Fri", "date": "30", "activity": ["KG Class Activity: Halloween", "KG and Classes I–XII – Parent Teacher Meeting (PTM)", "Term 1 Parent Teacher Meeting (PTM) – KG and Classes I–XII"] },
        { "day": "Sat", "date": "31", "activity": ["Term 1 Parent Teacher Meeting (PTM) – KG and Classes I–XII"] }
      ]
    },
    {
      "month_year": "November 2026",
      "general_activities": [
        "Class Competition – Dance Classes VI–IX",
        "Inter House Competition – Group Dance Classes III–XII",
        "Mela & MI Presentation – Group Dance Classes III–XII",
        "Term 2 Exhibition Group Commences for Classes III–VIII",
        "Class Competition – Fancy Dress Classes VI–XII"
      ],
      "daily_activities": [
        { "day": "Mon", "date": "02", "activity": [] },
        { "day": "Tue", "date": "03", "activity": ["KG Class Activity – Fresco Cooking"] },
        { "day": "Wed", "date": "04", "activity": [] },
        { "day": "Thu", "date": "05", "activity": ["KG Class Activity – Sparkling Diya"] },
        { "day": "Fri", "date": "06", "activity": [] },
        { "day": "Sat", "date": "07", "activity": ["Holiday KG – XII"] },
        { "day": "Mon", "date": "09", "activity": [] },
        { "day": "Tue", "date": "10", "activity": [] },
        { "day": "Wed", "date": "11", "activity": ["Revision Exam – I Classes X & XII Commences"] },
        { "day": "Thu", "date": "12", "activity": ["Working Day KG – IX, XI"] },
        { "day": "Fri", "date": "13", "activity": ["No Bag Day – KG–IX, XI", "Working Day KG – IX, XI"] },
        { "day": "Sat", "date": "14", "activity": ["KG Class Activity – Wonderful Creatures – Birds", "Revision Exam Classes X & XII Ends", "Term 2 Exhibition Display KG – XII"] },
        { "day": "Mon", "date": "16", "activity": ["UKG Dance Competition – Twist n Turn"] },
        { "day": "Tue", "date": "17", "activity": ["LKG Dance Competition – Twist n Turn"] },
        { "day": "Wed", "date": "18", "activity": ["Pre KG Dance Competition – Twist n Turn"] },
        { "day": "Thu", "date": "19", "activity": [] },
        { "day": "Fri", "date": "20", "activity": [] },
        { "day": "Sat", "date": "21", "activity": ["Library Week – Reading – A Window to the World Commences"] },
        { "day": "Mon", "date": "23", "activity": ["Library Week – Reading – A Window to the World", "No Bag Day KG & Classes I–II"] },
        { "day": "Tue", "date": "24", "activity": ["Library Week – Reading – A Window to the World Concludes", "Working Day Classes III–XII"] },
        { "day": "Wed", "date": "25", "activity": ["FA/PA / UT Commences"] },
        { "day": "Thu", "date": "26", "activity": ["Revision Exam 2 Classes X & XII Commences"] },
        { "day": "Fri", "date": "27", "activity": ["No Bag Day Classes III–XII", "Model Practical Exam Classes X & XII Commences"] },
        { "day": "Sat", "date": "28", "activity": ["Result Publication Revision Exam 1", "Custodian Meeting Classes III – VIII", "No Bag Day Classes I–II – VIII", "Working Day Classes III–XII"] },
        { "day": "Mon", "date": "30", "activity": ["Custodian Meeting Classes X & XII"] }
      ]
    },
    {
      "month_year": "December 2026",
      "general_activities": [
        "Class Competition – Dance Classes I–V",
        "Inter House Competition Classes I–V Plan Classes VI–X",
        "LKG Class Com",
        "LKG Class Competition"
      ],
      "daily_activities": [
        { "day": "Tue", "date": "01", "activity": ["Revision Exam 2 Classes X & XII Ends"] },
        { "day": "Wed", "date": "02", "activity": ["Result Publication FA/PA/UT"] },
        { "day": "Thu", "date": "03", "activity": [] },
        { "day": "Fri", "date": "04", "activity": [] },
        { "day": "Sat", "date": "05", "activity": [] },
        { "day": "Mon", "date": "07", "activity": ["Revision Exam 2 Classes X & XII Ends"] },
        { "day": "Tue", "date": "08", "activity": [] },
        { "day": "Wed", "date": "09", "activity": [] },
        { "day": "Thu", "date": "10", "activity": ["Term 2 Parent Interactive Meeting (PIM) – KG Classes & All Ends"] },
        { "day": "Fri", "date": "11", "activity": [] },
        { "day": "Sat", "date": "12", "activity": ["Term 2 Parent Interactive Meeting (PIM) – KG Classes – KG Class (LKG 3)"] },
        { "day": "Mon", "date": "14", "activity": ["Club Activities Classes III–VIII"] },
        { "day": "Tue", "date": "15", "activity": ["Working Day KG & Classes I–II"] },
        { "day": "Wed", "date": "16", "activity": ["Working Day KG – Classes I–II"] },
        { "day": "Thu", "date": "17", "activity": [] },
        { "day": "Fri", "date": "18", "activity": ["Result Publication FA/PA/UT"] },
        { "day": "Sat", "date": "19", "activity": [] },
        { "day": "Mon", "date": "21", "activity": ["Result Publication All Subjects Classes I–VIII", "Assignment Topic – Term 2 All Subjects Classes I–VIII"] },
        { "day": "Tue", "date": "22", "activity": [] },
        { "day": "Wed", "date": "23", "activity": ["Holiday – LKG Classes III–XI – Christmas Concert 25"] },
        { "day": "Thu", "date": "24", "activity": ["Working Day – KG Classes III, VIII – Working Day LKG Classes I, X, XII"] },
        { "day": "Fri", "date": "25", "activity": ["Holiday – Christmas"] },
        { "day": "Sat", "date": "26", "activity": [] },
        { "day": "Mon", "date": "28", "activity": [] },
        { "day": "Tue", "date": "29", "activity": [] },
        { "day": "Wed", "date": "30", "activity": ["Term Holiday Commences for Students"] },
        { "day": "Thu", "date": "31", "activity": ["Christmas"] }
      ]
    },
    {
      "month_year": "January 2027",
      "general_activities": [
        "Pre-Board Exam X & XI",
        "Inter House Competition – Quiz Classes IX–XI, XI",
        "Inter House Competition – Poster Classes VI–XI",
        "Intramural Competition Classes IX–XI",
        "Photo Classes Competition – Rangoli Celebration"
      ],
      "daily_activities": [
        { "day": "Fri", "date": "01", "activity": ["School Reopens for Students"] },
        { "day": "Sat", "date": "02", "activity": [] },
        { "day": "Mon", "date": "04", "activity": [] },
        { "day": "Tue", "date": "05", "activity": ["Assignment Submission Term 2 Classes I–VII", "Project Submission Classes IX, X & XII"] },
        { "day": "Wed", "date": "06", "activity": ["Holiday KG & Classes I–II", "Working Day Classes III–XII"] },
        { "day": "Thu", "date": "07", "activity": [] },
        { "day": "Fri", "date": "08", "activity": [] },
        { "day": "Sat", "date": "09", "activity": [] },
        { "day": "Mon", "date": "11", "activity": ["IKG Class Competition: Handwriting Competition"] },
        { "day": "Tue", "date": "12", "activity": [] },
        { "day": "Wed", "date": "13", "activity": ["KG Class Fest – Farm to Table – No Bag Day"] },
        { "day": "Thu", "date": "14", "activity": ["Holiday – Pongal"] },
        { "day": "Fri", "date": "15", "activity": ["Holiday – Thiruvalluvar Day"] },
        { "day": "Sat", "date": "16", "activity": ["Holiday – Uzhavar Thirunal"] },
        { "day": "Mon", "date": "18", "activity": [] },
        { "day": "Tue", "date": "19", "activity": ["KG Class Activity – Kiddies Royal Makeover – Prince & Princess"] },
        { "day": "Wed", "date": "20", "activity": ["No Bag Day KG – No Bag Day"] },
        { "day": "Thu", "date": "21", "activity": ["Term 2 PBL Commences for KG & Class I–II"] },
        { "day": "Fri", "date": "22", "activity": [] },
        { "day": "Sat", "date": "23", "activity": ["KG Class Activity – Fun with Clay – National Symbols"] },
        { "day": "Mon", "date": "25", "activity": ["Annual Sports Meet"] },
        { "day": "Tue", "date": "26", "activity": ["Holiday – Republic Day"] },
        { "day": "Wed", "date": "27", "activity": ["Farewell Grade XII"] },
        { "day": "Thu", "date": "28", "activity": [] },
        { "day": "Fri", "date": "29", "activity": ["Annual Class Photo Shoot", "Club Activities Classes III–VIII"] },
        { "day": "Sat", "date": "30", "activity": ["Annual Class Photo Shoot", "Working Day Classes I–XII"] },
        { "day": "Sun", "date": "31", "activity": ["Holiday KG, Working Day Classes I–XII"] }
      ]
    },
    {
      "month_year": "February 2027",
      "general_activities": [
        "Health Check-up Classes I–X",
        "Health Check-up Classes VI–X",
        "Health Check-up Classes IX–X",
        "KG & Classes I – X",
        "Working Day Classes I–XII"
      ],
      "daily_activities": [
        { "day": "Mon", "date": "01", "activity": ["Health Check-up KG & Classes I–V – Mid Term Exam Class I-V"] },
        { "day": "Tue", "date": "02", "activity": [] },
        { "day": "Wed", "date": "03", "activity": ["Health Check-up – KG – Classes I–V"] },
        { "day": "Thu", "date": "04", "activity": ["Health Check-up – VI – X"] },
        { "day": "Fri", "date": "05", "activity": ["Health Check-up – XI – XII"] },
        { "day": "Sat", "date": "06", "activity": ["Working Day Classes I–XII"] },
        { "day": "Mon", "date": "08", "activity": ["Health Check-up – KG"] },
        { "day": "Tue", "date": "09", "activity": [] },
        { "day": "Wed", "date": "10", "activity": [] },
        { "day": "Thu", "date": "11", "activity": ["FA/PA Commences"] },
        { "day": "Fri", "date": "12", "activity": ["No Bag Day – KG & Class I–XII"] },
        { "day": "Sat", "date": "13", "activity": [] },
        { "day": "Mon", "date": "15", "activity": ["KG Class Activity – Nature Trail"] },
        { "day": "Tue", "date": "16", "activity": ["No Bag Day – KG & Class I–VIII"] },
        { "day": "Wed", "date": "17", "activity": ["Term PBL Display KG & Class I–XII"] },
        { "day": "Thu", "date": "18", "activity": [] },
        { "day": "Fri", "date": "19", "activity": ["Year End Exam Classes IX and XI Ends"] },
        { "day": "Sat", "date": "20", "activity": ["KG Class Activity – Club Activities Classes III–VIII", "Holiday KG – XII"] },
        { "day": "Mon", "date": "22", "activity": ["Year End Exam Classes IX & XI Ends"] },
        { "day": "Tue", "date": "23", "activity": [] },
        { "day": "Wed", "date": "24", "activity": ["Portion Completion Classes I–VIII"] },
        { "day": "Thu", "date": "25", "activity": [] },
        { "day": "Fri", "date": "26", "activity": ["MON & ME Programme/Teacher Meeting (PTM) – Class I–IX, XI", "Term 2 Parent Teacher Meeting (PTM) – Classes I–IX, XI"] },
        { "day": "Sat", "date": "27", "activity": ["Result Declaration and Promotion List Classes I–VIII", "Term 2 Parent Teacher Meeting (PTM) – Classes I–IX, XI"] },
        { "day": "Mon", "date": "28", "activity": ["Holiday KG & Classes I–II", "Working Day Classes III–XII"] }
      ]
    },
    {
      "month_year": "March 2027",
      "general_activities": [
        "Leadership Camp Classes V–VI",
        "Revision Seminars for SA 2",
        "DAO and MI Report Classes VII–IX",
        "AY 2027–28 Classes Commences – No Bag Day – KG",
        "Fancy Dress Competition (Incredible India)"
      ],
      "daily_activities": [
        { "day": "Mon", "date": "01", "activity": ["Revision Commences for SA 2"] },
        { "day": "Tue", "date": "02", "activity": [] },
        { "day": "Wed", "date": "03", "activity": [] },
        { "day": "Thu", "date": "04", "activity": ["Parent Orientation AY 2027–28 Classes KG–IX"] },
        { "day": "Fri", "date": "05", "activity": ["Pre KG – No Bag Day"] },
        { "day": "Sat", "date": "06", "activity": ["Holiday KG & Classes I–XII"] },
        { "day": "Mon", "date": "08", "activity": ["LKG – Fancy Dress Competition (Incredible India)", "Holiday KG – XII"] },
        { "day": "Tue", "date": "09", "activity": ["UKG – Fancy Dress Competition (Incredible India)"] },
        { "day": "Wed", "date": "10", "activity": [] },
        { "day": "Thu", "date": "11", "activity": ["SA 2 Exam Commences Classes III–VII", "Diagnostic Assessment Commences"] },
        { "day": "Fri", "date": "12", "activity": [] },
        { "day": "Sat", "date": "13", "activity": [] },
        { "day": "Mon", "date": "15", "activity": ["KG – Fancy Dress Competition – Clay Modelling"] },
        { "day": "Tue", "date": "16", "activity": [] },
        { "day": "Wed", "date": "17", "activity": [] },
        { "day": "Thu", "date": "18", "activity": ["Holiday KG – Working Day Classes I–XII"] },
        { "day": "Fri", "date": "19", "activity": [] },
        { "day": "Sat", "date": "20", "activity": ["Wealth from Waste Competition – KG", "Holiday KG, Working Day Classes I–XII"] },
        { "day": "Mon", "date": "22", "activity": ["SA 2 Exam Ends", "Diagnostic Assessment Ends"] },
        { "day": "Tue", "date": "23", "activity": [] },
        { "day": "Wed", "date": "24", "activity": ["KG Event – Puppet Show", "Holiday KG, Working Day Classes I–XII"] },
        { "day": "Thu", "date": "25", "activity": [] },
        { "day": "Fri", "date": "26", "activity": ["Holiday – Good Friday"] },
        { "day": "Sat", "date": "27", "activity": [] },
        { "day": "Mon", "date": "29", "activity": [] },
        { "day": "Tue", "date": "30", "activity": [] },
        { "day": "Wed", "date": "31", "activity": ["KG Diagnostic Assessment Commences", "Result Publication SA1 (Continued)"] }
      ]
    },
    {
      "month_year": "April 2027",
      "general_activities": [
        "AY 2027–28 Commences Classes I–II",
        "Working KG & Classes I–II",
        "KG Diagnostic Assessment Ends",
        "Working Day Classes III–XII"
      ],
      "daily_activities": [
        { "day": "Thu", "date": "01", "activity": [] },
        { "day": "Fri", "date": "02", "activity": [] },
        { "day": "Sat", "date": "03", "activity": [] },
        { "day": "Mon", "date": "05", "activity": [] },
        { "day": "Tue", "date": "06", "activity": ["Holiday – Ugadi"] },
        { "day": "Wed", "date": "07", "activity": ["Holiday KG & Classes I–II", "Working Day Classes III–XII"] },
        { "day": "Thu", "date": "08", "activity": [] },
        { "day": "Fri", "date": "09", "activity": ["PA / Unit Test Classes 2 & XII Ends"] },
        { "day": "Sat", "date": "10", "activity": ["Quiz Competition (Unique India) – UKG & KG"] },
        { "day": "Mon", "date": "12", "activity": [] },
        { "day": "Tue", "date": "13", "activity": [] },
        { "day": "Wed", "date": "14", "activity": ["Holiday – Tamil New Year"] },
        { "day": "Thu", "date": "15", "activity": ["PA / Unit Test Classes X & XII Ends"] },
        { "day": "Fri", "date": "16", "activity": ["PA / Unit Test Classes X & XII Commences for KG", "Graduation Day LKG"] },
        { "day": "Sat", "date": "17", "activity": ["PA / Unit Test Classes X & XII Commences – KG", "Holiday Classes III–XII Working Day Classes I–II"] },
        { "day": "Mon", "date": "19", "activity": ["Summer Vacation Commences Classes I–VIII", "Holiday Class III Working Day Classes III–XII"] },
        { "day": "Tue", "date": "20", "activity": ["Assignment Topics – Term 1 All Subjects Classes I–VIII", "Record Work and Project Topics – Classes IX, X & 10"] },
        { "day": "Wed", "date": "21", "activity": [] },
        { "day": "Thu", "date": "22", "activity": ["PA / Unit Test Classes X & XII Ends – III – XII"] },
        { "day": "Fri", "date": "23", "activity": ["Holiday Classes I–III Working Day Classes III–XII"] },
        { "day": "Sat", "date": "24", "activity": ["Last Working Day for Classes – X & XII"] },
        { "day": "Mon", "date": "26", "activity": ["Result Publication SA1 X & XII Ends", "Summer Vacation Commences Classes III–IV"] },
        { "day": "Tue", "date": "27", "activity": [] },
        { "day": "Wed", "date": "28", "activity": ["Summer Vacation Commences for Classes – X & XII"] },
        { "day": "Thu", "date": "29", "activity": [] },
        { "day": "Fri", "date": "30", "activity": [] }
      ]
    }
  ]
}
```

- [ ] **Step 3: Verify JSON is valid**
```bash
node -e "require('./src/data/data.json'); console.log('valid')"
```
Expected: `valid`

- [ ] **Step 4: Commit**
```bash
git add src/data/data.json src/data/data-2025-26.json
git commit -m "feat: add 2026-27 data with extended schema"
```

---

## Task 3: Update realCalendarData.js

**Files:**
- Replace: `src/data/realCalendarData.js`

- [ ] **Step 1: Write new realCalendarData.js**

Replace entire file:

```js
import calendarData from './data.json';

const parseISODate = (str) => {
  const [y, m, d] = str.split('-').map(Number);
  return new Date(y, m - 1, d);
};

const parseMonthDate = (dateStr, monthIndex, year) => {
  const day = parseInt(dateStr);
  if (isNaN(day)) return null;
  return new Date(year, monthIndex, day);
};

// Map old/verbose category strings to the 7 canonical categories
const CATEGORY_MAP = {
  assembly: 'activity',
  competition: 'activity',
  trip: 'activity',
  academic: 'academic',
  exam: 'exam',
  holiday: 'holiday',
  vacation: 'vacation',
  fee: 'fee',
  ptm: 'ptm',
  activity: 'activity',
};

const inferCategory = (text) => {
  const t = text.toLowerCase();
  if (t.includes('holiday') || t.includes('bakrid') || t.includes('muharram') ||
      t.includes('christmas') || t.includes('pongal') || t.includes('republic day') ||
      t.includes('ugadi') || t.includes('good friday') || t.includes('tamil new year') ||
      t.includes('thiruvalluvar') || t.includes('uzhavar') || t.includes('onam') ||
      t.includes('diwali') || t.includes('independence day') || t.includes('gandhi')) {
    return 'holiday';
  }
  if (t.includes('vacation') || t.includes('summer holiday') || t.includes('term holiday')) return 'vacation';
  if (t.includes('exam') || t.includes(' test') || t.includes('assessment') ||
      t.includes('fa/pa') || t.includes('sa1') || t.includes('sa2') || t.includes('revision exam')) return 'exam';
  if (t.includes('ptm') || t.includes('parent teacher') || t.includes('parent interactive') ||
      t.includes('custodian meeting')) return 'ptm';
  if (t.includes('fee') || t.includes('payment')) return 'fee';
  if (t.includes('competition') || t.includes('trip') || t.includes('field trip') ||
      t.includes('assembly') || t.includes('club') || t.includes('talent show') ||
      t.includes('exhibition') || t.includes('sports') || t.includes('athletic') ||
      t.includes('dance') || t.includes('excursion') || t.includes('ceremony') ||
      t.includes('investiture') || t.includes('graduation')) return 'activity';
  return 'academic';
};

const extractClasses = (text) => {
  const m = text.match(/classes?\s+([IVX\-\d,\s&KG]+)/i);
  if (m) return m[1].replace(/\s+/g, ' ').trim();
  if (text.toLowerCase().includes('kg')) return 'KG';
  return 'All Classes';
};

let _id = 1;
const nextId = () => _id++;

// --- Academic calendar ---
const fromAcademicCalendar = () => {
  const events = [];
  calendarData.academic_calendar.forEach(({ month_year, general_activities = [], daily_activities = [] }) => {
    const [monthName, yearStr] = month_year.split(' ');
    const year = parseInt(yearStr);
    const monthIndex = new Date(Date.parse(monthName + ' 1, 2000')).getMonth();

    general_activities.forEach((act) => {
      events.push({
        id: nextId(),
        title: act.replace(/\[cite:.*?\]/g, '').trim(),
        date: new Date(year, monthIndex, 1),
        category: inferCategory(act),
        description: `Monthly activity for ${month_year}`,
        classes: extractClasses(act),
        isGeneralActivity: true,
      });
    });

    daily_activities.forEach(({ date: dateStr, day, activity: acts }) => {
      const date = parseMonthDate(dateStr, monthIndex, year);
      if (!date) return;
      acts.forEach((act) => {
        events.push({
          id: nextId(),
          title: act.replace(/\[cite:.*?\]/g, '').trim(),
          date,
          category: inferCategory(act),
          description: `${dateStr} ${month_year}`,
          classes: extractClasses(act),
          dayOfWeek: day,
        });
      });
    });
  });
  return events;
};

// --- Holidays (authoritative) ---
const fromHolidays = () =>
  calendarData.holidays.map((h) => ({
    id: nextId(),
    title: h.name,
    date: parseISODate(h.date),
    category: 'holiday',
    description: 'Public Holiday',
    classes: 'All Classes',
  }));

// --- Vacations ---
const fromVacations = () =>
  calendarData.vacations.map((v) => ({
    id: nextId(),
    title: v.name,
    date: parseISODate(v.start),
    endDate: parseISODate(v.end),
    category: 'vacation',
    description: `Vacation for ${v.applicable_to}`,
    classes: v.applicable_to,
  }));

// --- Special assemblies → activity ---
const fromAssemblies = () =>
  calendarData.special_assemblies.map((a) => ({
    id: nextId(),
    title: a.topic,
    date: parseISODate(a.date),
    category: 'activity',
    description: 'Special Assembly',
    classes: 'All Classes',
  }));

// --- PTM schedule ---
const fromPTM = () => {
  const events = [];
  calendarData.ptm_schedule.forEach((p) => {
    p.dates.forEach((d) => {
      events.push({
        id: nextId(),
        title: p.type,
        date: parseISODate(d),
        category: 'ptm',
        description: `Term ${p.term} — ${p.class_range}`,
        classes: p.class_range,
        ptmType: p.type,
        term: p.term,
      });
    });
  });
  return events;
};

// --- Fee schedule ---
const fromFees = () =>
  calendarData.fee_schedule.map((f) => ({
    id: nextId(),
    title: `Fee Deadline – ${f.term} (${f.class_range})`,
    date: parseISODate(f.cutoff_end),
    category: 'fee',
    description: `${f.term} fee payment cutoff`,
    classes: f.class_range,
    feeAmount: f.amount,
    feeTerm: f.term,
    feeCutoffStart: f.cutoff_start,
    feeCutoffEnd: f.cutoff_end,
  }));

// --- Exam schedule ---
const fromExams = () =>
  calendarData.exam_schedule.map((e) => ({
    id: nextId(),
    title: e.assessment,
    date: parseISODate(e.date_start),
    endDate: parseISODate(e.date_end),
    category: 'exam',
    description: `${e.assessment} — ${e.class_range}`,
    classes: e.class_range,
  }));

export const schoolTimings = calendarData.school_timings;

export const allCalendarEvents = [
  ...fromAcademicCalendar(),
  ...fromHolidays(),
  ...fromVacations(),
  ...fromAssemblies(),
  ...fromPTM(),
  ...fromFees(),
  ...fromExams(),
].sort((a, b) => new Date(a.date) - new Date(b.date));

export default allCalendarEvents;
```

- [ ] **Step 2: Verify app compiles**
```bash
CI=false npm run build 2>&1 | grep -E "error|warning|compiled" | tail -10
```
Expected: `Successfully compiled` with no errors.

- [ ] **Step 3: Commit**
```bash
git add src/data/realCalendarData.js
git commit -m "feat: update realCalendarData for 2026-27 schema and 7-category model"
```

---

## Task 4: Category Consolidation — Update All UI Components

**Files:**
- Modify: `src/config/appConfig.js`
- Modify: `src/App.js`
- Modify: `src/components/Dashboard.js`
- Modify: `src/components/SectionsView.js`
- Modify: `src/components/MobileAgenda.js`

- [ ] **Step 1: Update appConfig.js colors**

In `src/config/appConfig.js`, replace the `theme.colors` block:
```js
colors: {
  academic: '#3b82f6',   // blue-500
  exam:     '#8b5cf6',   // violet-500
  activity: '#06b6d4',   // cyan-500
  holiday:  '#ef4444',   // red-500
  vacation: '#f59e0b',   // amber-500
  fee:      '#dc2626',   // red-600
  ptm:      '#7c3aed',   // violet-700
},
```
Also update `school.academicYear` to `'2026-2027'`.

- [ ] **Step 2: Update App.js eventStyleGetter and filterButtons**

In `src/App.js`, replace `eventStyleGetter` switch cases:
```js
const eventStyleGetter = (event) => {
  const colors = {
    academic: '#3b82f6',
    exam:     '#8b5cf6',
    activity: '#06b6d4',
    holiday:  '#ef4444',
    vacation: '#f59e0b',
    fee:      '#dc2626',
    ptm:      '#7c3aed',
  };
  return {
    style: {
      backgroundColor: colors[event.category] || '#6b7280',
      borderRadius: '4px', opacity: 0.8, color: 'white', border: '0px', display: 'block',
    },
  };
};
```

Replace `filterButtons` array:
```js
const filterButtons = [
  { key: 'all',      label: 'All',       icon: CalendarIcon, color: 'bg-gray-500' },
  { key: 'academic', label: 'Academic',  icon: BookOpen,     color: 'bg-blue-500' },
  { key: 'exam',     label: 'Exams',     icon: GraduationCap,color: 'bg-violet-500' },
  { key: 'activity', label: 'Activities',icon: Zap,          color: 'bg-cyan-500' },
  { key: 'holiday',  label: 'Holidays',  icon: Home,         color: 'bg-red-500' },
  { key: 'vacation', label: 'Vacation',  icon: Clock,        color: 'bg-amber-500' },
  { key: 'fee',      label: 'Fee',       icon: Download,     color: 'bg-red-600' },
  { key: 'ptm',      label: 'PTM',       icon: Users,        color: 'bg-violet-700' },
];
```

Remove unused lucide imports: `Trophy`, `MapPin`, `BarChart3`, `Grid`, `List`, `Search` (Search will be added back in Task 8). Keep the ones referenced above. Add `Zap` if not already imported.

- [ ] **Step 3: Update Dashboard.js**

Replace the stats object inside `useMemo` to use the 7 categories:
```js
const stats = useMemo(() => {
  const count = (cat) => events.filter((e) => e.category === cat).length;
  const today = moment();

  const upcomingEvents = events.filter((e) =>
    moment(e.date).isAfter(today) && moment(e.date).isBefore(moment().add(30, 'days'))
  ).length;

  const thisMonthEvents = events.filter((e) => {
    const d = moment(e.date);
    return d.month() === today.month() && d.year() === today.year();
  }).length;

  const nextVacation = events
    .filter((e) => e.category === 'vacation')
    .find((e) => moment(e.date).isAfter(today));

  return {
    academic:  count('academic'),
    exams:     count('exam'),
    activities:count('activity'),
    holidays:  count('holiday'),
    vacations: count('vacation'),
    fees:      count('fee'),
    ptm:       count('ptm'),
    upcomingEvents,
    thisMonthEvents,
    daysUntilVacation: nextVacation ? moment(nextVacation.date).diff(today, 'days') : null,
  };
}, [events]);
```

Update the JSX stat cards to reference the new keys (`academic`, `exams`, `activities`, `holidays`).

- [ ] **Step 4: Update SectionsView.js**

Replace sections array:
```js
const sections = [
  { title: 'Academic Events',   events: events.filter(e => e.category === 'academic'  && isUpcoming(e)), icon: BookOpen,      color: 'bg-blue-600',   description: 'Academic milestones and educational activities' },
  { title: 'Exams',             events: events.filter(e => e.category === 'exam'       && isUpcoming(e)), icon: GraduationCap,color: 'bg-violet-500', description: 'Tests, assessments and examinations' },
  { title: 'Activities',        events: events.filter(e => e.category === 'activity'   && isUpcoming(e)), icon: Zap,          color: 'bg-cyan-500',   description: 'Competitions, assemblies, trips and clubs' },
  { title: 'Holidays',          events: events.filter(e => e.category === 'holiday'    && isUpcoming(e)), icon: Home,         color: 'bg-red-500',    description: 'Public and school holidays' },
  { title: 'Vacation',          events: events.filter(e => e.category === 'vacation'   && isUpcoming(e)), icon: Clock,        color: 'bg-amber-500',  description: 'Vacation periods' },
  { title: 'Fee Deadlines',     events: events.filter(e => e.category === 'fee'        && isUpcoming(e)), icon: Download,     color: 'bg-red-600',    description: 'Fee payment cutoff dates' },
  { title: 'PTM',               events: events.filter(e => e.category === 'ptm'        && isUpcoming(e)), icon: Users,        color: 'bg-violet-700', description: 'Parent-teacher meetings' },
];
```

Update imports to include `GraduationCap`, `Zap`, `Download` from lucide-react.

- [ ] **Step 5: Update MobileAgenda.js getCategoryColor**

Replace the colors map in `getCategoryColor`:
```js
const getCategoryColor = (category) => {
  const colors = {
    holiday:  'bg-red-100 text-red-800 border-red-200',
    vacation: 'bg-amber-100 text-amber-800 border-amber-200',
    academic: 'bg-blue-100 text-blue-800 border-blue-200',
    exam:     'bg-violet-100 text-violet-800 border-violet-200',
    activity: 'bg-cyan-100 text-cyan-800 border-cyan-200',
    fee:      'bg-red-200 text-red-900 border-red-300',
    ptm:      'bg-violet-200 text-violet-900 border-violet-300',
  };
  return colors[category] || 'bg-gray-100 text-gray-800 border-gray-200';
};
```

- [ ] **Step 6: Verify build**
```bash
CI=false npm run build 2>&1 | grep -E "^(ERROR|Failed)" | head -5
```
Expected: no output (no errors).

- [ ] **Step 7: Commit**
```bash
git add src/config/appConfig.js src/App.js src/components/Dashboard.js src/components/SectionsView.js src/components/MobileAgenda.js
git commit -m "feat: consolidate to 7 event categories"
```

---

## Task 5: TimingsView Component + Timings Tab

**Files:**
- Create: `src/components/TimingsView.js`
- Modify: `src/App.js`

- [ ] **Step 1: Create TimingsView.js**

```jsx
import { Clock } from 'lucide-react';
import { schoolTimings } from '../data/realCalendarData';

const TimingCard = ({ group }) => (
  <div className="bg-white rounded-xl shadow-md p-5">
    <div className="flex items-center justify-between mb-3">
      <h3 className="text-lg font-bold text-gray-900">{group.group}</h3>
      <span className="text-sm text-gray-500 bg-gray-100 px-2 py-1 rounded-full">
        {group.start} – {group.end}
      </span>
    </div>
    <p className="text-xs text-gray-500 mb-3">{group.days}</p>
    {group.periods.length > 0 && (
      <table className="w-full text-sm">
        <tbody>
          {group.periods.map((p, i) => (
            <tr key={i} className={i % 2 === 0 ? 'bg-gray-50' : ''}>
              <td className="py-1 px-2 text-gray-700 font-medium w-1/2">{p.label}</td>
              <td className="py-1 px-2 text-gray-600">{p.time}</td>
            </tr>
          ))}
        </tbody>
      </table>
    )}
  </div>
);

const TimingsView = () => {
  const mainGroups = schoolTimings.filter(
    (g) => !g.group.startsWith('Office') && !g.group.startsWith('After')
  );
  const infoGroups = schoolTimings.filter(
    (g) => g.group.startsWith('Office') || g.group.startsWith('After')
  );

  return (
    <div className="space-y-6">
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {mainGroups.map((g) => (
          <TimingCard key={g.group} group={g} />
        ))}
      </div>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        {infoGroups.map((g) => (
          <div key={g.group} className="bg-blue-50 rounded-lg p-4 flex items-center gap-3">
            <Clock className="h-5 w-5 text-blue-600 flex-shrink-0" />
            <div>
              <p className="font-semibold text-blue-900 text-sm">{g.group}</p>
              <p className="text-blue-700 text-sm">{g.start} – {g.end}</p>
              <p className="text-blue-500 text-xs">{g.days}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
};

export default TimingsView;
```

- [ ] **Step 2: Add Timings tab to App.js view mode toggle**

In `src/App.js`, add import:
```js
import TimingsView from './components/TimingsView';
```

Change `viewMode` state default if needed — keep `'calendar'`. Add a third button to the view-mode toggle group:
```jsx
<button
  onClick={() => { setViewMode('timings'); trackViewModeChange('timings'); }}
  className={`flex items-center px-2 md:px-3 py-1 rounded-md text-xs md:text-sm font-medium transition-all duration-200 whitespace-nowrap ${
    viewMode === 'timings' ? 'bg-white text-gray-900 shadow-sm' : 'text-gray-600 hover:text-gray-900'
  }`}
>
  <Clock size={14} className="mr-1" />
  Timings
</button>
```

Add import for `Clock` from lucide-react if not already imported.

In the main grid section, add a third condition:
```jsx
} : viewMode === 'timings' ? (
  <div className="lg:col-span-4">
    <TimingsView />
  </div>
) : (
```

- [ ] **Step 3: Verify build**
```bash
CI=false npm run build 2>&1 | grep -E "^(ERROR|Failed)" | head -5
```
Expected: no output.

- [ ] **Step 4: Commit**
```bash
git add src/components/TimingsView.js src/App.js
git commit -m "feat: add school timings view"
```

---

## Task 6: Fee Details in EventModal

**Files:**
- Modify: `src/components/EventModal.js`

- [ ] **Step 1: Update EventModal to show fee and PTM details**

In `src/components/EventModal.js`, update `getEventIcon` to handle all 7 categories:
```js
const getEventIcon = (type) => {
  const icons = {
    holiday:  <Calendar className="h-6 w-6 text-red-500" />,
    vacation: <Clock className="h-6 w-6 text-amber-500" />,
    academic: <BookOpen className="h-6 w-6 text-blue-500" />,
    exam:     <BookOpen className="h-6 w-6 text-violet-500" />,
    activity: <Users className="h-6 w-6 text-cyan-500" />,
    fee:      <Download className="h-6 w-6 text-red-600" />,
    ptm:      <Users className="h-6 w-6 text-violet-700" />,
  };
  return icons[type] || <Calendar className="h-6 w-6 text-gray-500" />;
};
```

Add import for `Download` from lucide-react.

Update `getEventTypeLabel`:
```js
const labels = {
  holiday: 'Holiday', vacation: 'Vacation', academic: 'Academic Event',
  exam: 'Exam / Assessment', activity: 'Activity', fee: 'Fee Deadline', ptm: 'Parent Meeting',
};
return labels[type] || 'Event';
```

After the `event.notes` block, add fee details section:
```jsx
{event.category === 'fee' && (
  <div className="mt-4 bg-red-50 rounded-lg p-4">
    <h3 className="text-sm font-semibold text-red-900 mb-2">Fee Details</h3>
    <div className="space-y-1 text-sm text-red-800">
      <p><span className="font-medium">Term:</span> {event.feeTerm}</p>
      <p><span className="font-medium">Classes:</span> {event.classes}</p>
      <p><span className="font-medium">Amount:</span> ₹{event.feeAmount?.toLocaleString('en-IN')}</p>
      <p><span className="font-medium">Payment window:</span> {event.feeCutoffStart} – {event.feeCutoffEnd}</p>
    </div>
  </div>
)}
{event.category === 'ptm' && (
  <div className="mt-4 bg-violet-50 rounded-lg p-4">
    <h3 className="text-sm font-semibold text-violet-900 mb-2">Meeting Details</h3>
    <div className="space-y-1 text-sm text-violet-800">
      <p><span className="font-medium">Type:</span> {event.ptmType}</p>
      <p><span className="font-medium">Classes:</span> {event.classes}</p>
      {event.term && <p><span className="font-medium">Term:</span> {event.term}</p>}
    </div>
  </div>
)}
```

- [ ] **Step 2: Verify build**
```bash
CI=false npm run build 2>&1 | grep -E "^(ERROR|Failed)" | head -5
```
Expected: no output.

- [ ] **Step 3: Commit**
```bash
git add src/components/EventModal.js
git commit -m "feat: show fee and PTM details in event modal"
```

---

## Task 7: SearchModal Component

**Files:**
- Create: `src/components/SearchModal.js`

- [ ] **Step 1: Create SearchModal.js**

```jsx
import { Search, X } from 'lucide-react';
import moment from 'moment';
import { useEffect, useMemo, useRef, useState } from 'react';

const CATEGORY_COLORS = {
  academic: 'bg-blue-500', exam: 'bg-violet-500', activity: 'bg-cyan-500',
  holiday: 'bg-red-500', vacation: 'bg-amber-500', fee: 'bg-red-600', ptm: 'bg-violet-700',
};

const SearchModal = ({ events, onEventClick, onClose }) => {
  const [query, setQuery] = useState('');
  const [highlighted, setHighlighted] = useState(0);
  const inputRef = useRef(null);
  const listRef = useRef(null);

  useEffect(() => { inputRef.current?.focus(); }, []);

  useEffect(() => {
    const handler = (e) => { if (e.key === 'Escape') onClose(); };
    window.addEventListener('keydown', handler);
    return () => window.removeEventListener('keydown', handler);
  }, [onClose]);

  const results = useMemo(() => {
    if (query.trim().length < 1) return [];
    const q = query.toLowerCase();
    return events
      .filter((e) =>
        e.title?.toLowerCase().includes(q) ||
        e.description?.toLowerCase().includes(q) ||
        e.classes?.toLowerCase().includes(q)
      )
      .slice(0, 40);
  }, [query, events]);

  // Group by month
  const grouped = useMemo(() => {
    const map = {};
    results.forEach((e) => {
      const key = moment(e.date).format('MMMM YYYY');
      if (!map[key]) map[key] = [];
      map[key].push(e);
    });
    return Object.entries(map);
  }, [results]);

  const flatResults = results;

  const handleKey = (e) => {
    if (e.key === 'ArrowDown') {
      e.preventDefault();
      setHighlighted((h) => Math.min(h + 1, flatResults.length - 1));
    } else if (e.key === 'ArrowUp') {
      e.preventDefault();
      setHighlighted((h) => Math.max(h - 1, 0));
    } else if (e.key === 'Enter' && flatResults[highlighted]) {
      onEventClick(flatResults[highlighted]);
      onClose();
    }
  };

  let resultIndex = 0;

  return (
    <div
      className="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-start justify-center pt-[10vh] px-4"
      onClick={onClose}
    >
      <div
        className="bg-white rounded-xl shadow-2xl w-full max-w-xl flex flex-col max-h-[75vh]"
        onClick={(e) => e.stopPropagation()}
      >
        {/* Input */}
        <div className="flex items-center px-4 py-3 border-b border-gray-200">
          <Search className="h-5 w-5 text-gray-400 mr-3 flex-shrink-0" />
          <input
            ref={inputRef}
            type="text"
            placeholder="Search events..."
            value={query}
            onChange={(e) => { setQuery(e.target.value); setHighlighted(0); }}
            onKeyDown={handleKey}
            className="flex-1 text-base outline-none text-gray-900 placeholder-gray-400"
          />
          <button onClick={onClose} className="text-gray-400 hover:text-gray-600 ml-2">
            <X size={18} />
          </button>
        </div>

        {/* Results */}
        <div ref={listRef} className="overflow-y-auto flex-1">
          {query.length < 1 && (
            <p className="text-sm text-gray-400 text-center py-8">Start typing to search…</p>
          )}
          {query.length >= 1 && results.length === 0 && (
            <p className="text-sm text-gray-400 text-center py-8">No events match "{query}"</p>
          )}
          {grouped.map(([month, monthEvents]) => (
            <div key={month}>
              <p className="text-xs font-semibold text-gray-400 uppercase tracking-wider px-4 py-2 bg-gray-50 sticky top-0">
                {month}
              </p>
              {monthEvents.map((event) => {
                const idx = resultIndex++;
                const isHighlighted = idx === highlighted;
                return (
                  <button
                    key={event.id}
                    onClick={() => { onEventClick(event); onClose(); }}
                    onMouseEnter={() => setHighlighted(idx)}
                    className={`w-full text-left flex items-center gap-3 px-4 py-2.5 transition-colors ${
                      isHighlighted ? 'bg-primary-50' : 'hover:bg-gray-50'
                    }`}
                  >
                    <span className={`w-2 h-2 rounded-full flex-shrink-0 ${CATEGORY_COLORS[event.category] || 'bg-gray-400'}`} />
                    <span className="text-xs text-gray-400 w-24 flex-shrink-0">
                      {moment(event.date).format('ddd D MMM')}
                    </span>
                    <span className="text-sm text-gray-900 truncate">{event.title}</span>
                  </button>
                );
              })}
            </div>
          ))}
        </div>

        {/* Footer hint */}
        {results.length > 0 && (
          <div className="px-4 py-2 border-t border-gray-100 flex gap-4 text-xs text-gray-400">
            <span>↑↓ navigate</span><span>↵ open</span><span>esc close</span>
          </div>
        )}
      </div>
    </div>
  );
};

export default SearchModal;
```

- [ ] **Step 2: Verify it compiles (import check)**
```bash
CI=false npm run build 2>&1 | grep -E "^(ERROR|Failed)" | head -5
```
Expected: no output (SearchModal isn't wired yet, but import chains are clean).

- [ ] **Step 3: Commit**
```bash
git add src/components/SearchModal.js
git commit -m "feat: add CMD+K SearchModal component"
```

---

## Task 8: Wire Search into App.js

**Files:**
- Modify: `src/App.js`

- [ ] **Step 1: Add SearchModal state and ⌘K listener**

In `src/App.js`, add import:
```js
import SearchModal from './components/SearchModal';
```

Add state:
```js
const [showSearch, setShowSearch] = useState(false);
```

Add `useEffect` for keyboard shortcut (alongside the existing analytics `useEffect`):
```js
useEffect(() => {
  const handler = (e) => {
    if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
      e.preventDefault();
      setShowSearch(true);
    }
  };
  window.addEventListener('keydown', handler);
  return () => window.removeEventListener('keydown', handler);
}, []);
```

- [ ] **Step 2: Replace search input with trigger button**

Find the existing search `<input>` block in the control bar and replace with:
```jsx
<button
  onClick={() => setShowSearch(true)}
  className="relative flex items-center w-full max-w-md px-4 py-2 md:py-2.5 text-sm text-gray-400 bg-white border border-gray-300 rounded-lg hover:border-gray-400 transition-colors"
>
  <Search className="h-4 w-4 mr-2 flex-shrink-0" />
  <span>Search events…</span>
  <span className="ml-auto text-xs bg-gray-100 px-1.5 py-0.5 rounded">⌘K</span>
</button>
```

Add `Search` to lucide-react imports.

- [ ] **Step 3: Remove inline search state and filter logic**

Remove:
- `const [searchTerm, setSearchTerm] = useState('');`
- The `searchTerm` debounce `useEffect`
- The `searchTerm` filter inside `filteredEvents` useMemo (keep the `activeFilter` filter)

- [ ] **Step 4: Render SearchModal**

Just before `</div>` at the bottom of the return (alongside the EventModal):
```jsx
{showSearch && (
  <SearchModal
    events={events}
    onEventClick={(event) => { setSelectedEvent(event); }}
    onClose={() => setShowSearch(false)}
  />
)}
```

- [ ] **Step 5: Verify build and smoke test**
```bash
CI=false npm run build 2>&1 | grep -E "^(ERROR|Failed)" | head -5
```
Expected: no output.

- [ ] **Step 6: Commit**
```bash
git add src/App.js
git commit -m "feat: wire CMD+K search into App"
```

---

## Task 9: Service Worker

**Files:**
- Create: `public/sw.js`

- [ ] **Step 1: Create public/sw.js**

```js
const DB_NAME = 'school_cal_notifs';
const STORE_NAME = 'scheduled';

const openDB = () =>
  new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, 1);
    req.onupgradeneeded = () => req.result.createObjectStore(STORE_NAME, { keyPath: 'tag' });
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });

const saveNotif = async (notif) => {
  const db = await openDB();
  return new Promise((res, rej) => {
    const tx = db.transaction(STORE_NAME, 'readwrite');
    tx.objectStore(STORE_NAME).put(notif);
    tx.oncomplete = res;
    tx.onerror = rej;
  });
};

const getAllNotifs = async () => {
  const db = await openDB();
  return new Promise((res, rej) => {
    const tx = db.transaction(STORE_NAME, 'readonly');
    const req = tx.objectStore(STORE_NAME).getAll();
    req.onsuccess = () => res(req.result);
    req.onerror = rej;
  });
};

const deleteNotif = async (tag) => {
  const db = await openDB();
  return new Promise((res, rej) => {
    const tx = db.transaction(STORE_NAME, 'readwrite');
    tx.objectStore(STORE_NAME).delete(tag);
    tx.oncomplete = res;
    tx.onerror = rej;
  });
};

// Fire pending notifications that are due
const firePending = async () => {
  const now = Date.now();
  const all = await getAllNotifs();
  for (const n of all) {
    if (n.fireAt <= now) {
      await self.registration.showNotification(n.title, {
        body: n.body,
        icon: '/logo192.png',
        tag: n.tag,
        data: { url: '/' },
      });
      await deleteNotif(n.tag);
    }
  }
};

self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', (e) => {
  e.waitUntil(self.clients.claim().then(firePending));
});

self.addEventListener('message', async (e) => {
  if (e.data?.type === 'SCHEDULE') {
    const { title, body, fireAt, tag } = e.data;
    const delay = fireAt - Date.now();
    if (delay <= 0) {
      // Fire immediately
      await self.registration.showNotification(title, {
        body, icon: '/logo192.png', tag, data: { url: '/' },
      });
    } else if (delay < 86400000) {
      // Within 24h — use setTimeout
      setTimeout(async () => {
        await self.registration.showNotification(title, {
          body, icon: '/logo192.png', tag, data: { url: '/' },
        });
      }, delay);
    } else {
      // Longer — persist to IndexedDB, fire on next SW activation
      await saveNotif({ tag, title, body, fireAt });
    }
  }

  if (e.data?.type === 'CLEAR_ALL') {
    const all = await getAllNotifs();
    for (const n of all) await deleteNotif(n.tag);
  }
});

self.addEventListener('notificationclick', (e) => {
  e.notification.close();
  e.waitUntil(
    self.clients.matchAll({ type: 'window' }).then((clients) => {
      const focused = clients.find((c) => c.focus);
      if (focused) return focused.focus();
      return self.clients.openWindow('/');
    })
  );
});
```

- [ ] **Step 2: Commit**
```bash
git add public/sw.js
git commit -m "feat: add notification service worker"
```

---

## Task 10: useNotifications Hook

**Files:**
- Create: `src/hooks/useNotifications.js`

- [ ] **Step 1: Create src/hooks/useNotifications.js**

```js
import { useCallback, useEffect, useState } from 'react';

const PREFS_KEY = 'school_cal_notif_prefs';

const DEFAULT_PREFS = {
  enabled: false,
  categories: ['academic', 'exam', 'activity', 'holiday', 'vacation', 'fee', 'ptm'],
  advanceDays: [1],
};

const loadPrefs = () => {
  try {
    const raw = localStorage.getItem(PREFS_KEY);
    return raw ? { ...DEFAULT_PREFS, ...JSON.parse(raw) } : DEFAULT_PREFS;
  } catch {
    return DEFAULT_PREFS;
  }
};

const savePrefs = (prefs) => localStorage.setItem(PREFS_KEY, JSON.stringify(prefs));

const getFireTime = (eventDate, advanceDays) => {
  const d = new Date(eventDate);
  d.setDate(d.getDate() - advanceDays);
  d.setHours(8, 0, 0, 0);
  return d.getTime();
};

const bodyText = (advanceDays, dateStr) => {
  if (advanceDays === 0) return `Today — ${dateStr}`;
  if (advanceDays === 1) return `Tomorrow — ${dateStr}`;
  return `In ${advanceDays} days — ${dateStr}`;
};

let swReg = null;

const registerSW = async () => {
  if (!('serviceWorker' in navigator)) return null;
  if (swReg) return swReg;
  swReg = await navigator.serviceWorker.register('/sw.js');
  await navigator.serviceWorker.ready;
  return swReg;
};

const scheduleAll = async (events, prefs) => {
  const reg = await registerSW();
  if (!reg?.active) return;

  // Clear previous schedules
  reg.active.postMessage({ type: 'CLEAR_ALL' });

  if (!prefs.enabled) return;

  const now = Date.now();
  events.forEach((event) => {
    if (!prefs.categories.includes(event.category)) return;
    prefs.advanceDays.forEach((days) => {
      const fireAt = getFireTime(event.date, days);
      if (fireAt <= now) return; // already past
      const tag = `event-${event.id}-${days}`;
      const dateStr = new Date(event.date).toLocaleDateString('en-IN', { day: 'numeric', month: 'short' });
      reg.active.postMessage({
        type: 'SCHEDULE',
        tag,
        title: event.title,
        body: bodyText(days, dateStr),
        fireAt,
      });
    });
  });
};

export const useNotifications = (events) => {
  const [prefs, setPrefs] = useState(loadPrefs);
  const [permissionStatus, setPermissionStatus] = useState(
    'Notification' in window ? Notification.permission : 'unsupported'
  );

  useEffect(() => {
    if (prefs.enabled && permissionStatus === 'granted') {
      scheduleAll(events, prefs);
    }
  }, [events, prefs, permissionStatus]);

  const requestPermission = useCallback(async () => {
    if (!('Notification' in window)) return;
    const result = await Notification.requestPermission();
    setPermissionStatus(result);
    if (result === 'granted') {
      const newPrefs = { ...prefs, enabled: true };
      setPrefs(newPrefs);
      savePrefs(newPrefs);
    }
  }, [prefs]);

  const updatePrefs = useCallback((updates) => {
    const newPrefs = { ...prefs, ...updates };
    setPrefs(newPrefs);
    savePrefs(newPrefs);
  }, [prefs]);

  return { prefs, updatePrefs, permissionStatus, requestPermission };
};
```

- [ ] **Step 2: Commit**
```bash
git add src/hooks/useNotifications.js
git commit -m "feat: add useNotifications hook"
```

---

## Task 11: NotificationSettings Component + Header Bell

**Files:**
- Create: `src/components/NotificationSettings.js`
- Modify: `src/components/Header.js`
- Modify: `src/App.js`

- [ ] **Step 1: Create NotificationSettings.js**

```jsx
import { Bell, BellOff, X } from 'lucide-react';

const CATEGORIES = [
  { key: 'academic', label: 'Academic',   color: 'bg-blue-500' },
  { key: 'exam',     label: 'Exams',      color: 'bg-violet-500' },
  { key: 'activity', label: 'Activities', color: 'bg-cyan-500' },
  { key: 'holiday',  label: 'Holidays',   color: 'bg-red-500' },
  { key: 'vacation', label: 'Vacation',   color: 'bg-amber-500' },
  { key: 'fee',      label: 'Fee',        color: 'bg-red-600' },
  { key: 'ptm',      label: 'PTM',        color: 'bg-violet-700' },
];

const ADVANCE_OPTIONS = [
  { days: 0, label: 'Day of (8 AM)' },
  { days: 1, label: '1 day before' },
  { days: 3, label: '3 days before' },
  { days: 7, label: '1 week before' },
];

const NotificationSettings = ({ prefs, updatePrefs, permissionStatus, requestPermission, onClose }) => {
  const toggleCategory = (key) => {
    const cats = prefs.categories.includes(key)
      ? prefs.categories.filter((c) => c !== key)
      : [...prefs.categories, key];
    updatePrefs({ categories: cats });
  };

  const toggleAdvance = (days) => {
    const adv = prefs.advanceDays.includes(days)
      ? prefs.advanceDays.filter((d) => d !== days)
      : [...prefs.advanceDays, days];
    updatePrefs({ advanceDays: adv });
  };

  return (
    <div className="fixed inset-0 bg-black bg-opacity-40 z-50 flex items-start justify-end pt-20 pr-4 sm:pr-8" onClick={onClose}>
      <div className="bg-white rounded-xl shadow-2xl w-full max-w-sm p-5" onClick={(e) => e.stopPropagation()}>
        <div className="flex items-center justify-between mb-4">
          <h2 className="text-lg font-bold text-gray-900 flex items-center gap-2">
            <Bell className="h-5 w-5 text-primary-600" /> Notifications
          </h2>
          <button onClick={onClose}><X size={18} className="text-gray-400 hover:text-gray-600" /></button>
        </div>

        {/* Permission status */}
        {permissionStatus === 'unsupported' && (
          <p className="text-sm text-red-600 mb-4">Your browser doesn't support notifications.</p>
        )}
        {permissionStatus === 'denied' && (
          <p className="text-sm text-red-600 mb-4">Notifications blocked. Enable them in your browser's site settings, then reload.</p>
        )}
        {permissionStatus === 'default' && (
          <button
            onClick={requestPermission}
            className="w-full mb-4 bg-primary-600 text-white py-2 px-4 rounded-lg text-sm font-medium hover:bg-primary-700"
          >
            Enable browser notifications
          </button>
        )}

        {/* Master toggle */}
        {permissionStatus === 'granted' && (
          <label className="flex items-center justify-between mb-4 cursor-pointer">
            <span className="text-sm font-medium text-gray-700">Notifications enabled</span>
            <button
              onClick={() => updatePrefs({ enabled: !prefs.enabled })}
              className={`relative w-10 h-5 rounded-full transition-colors ${prefs.enabled ? 'bg-primary-600' : 'bg-gray-300'}`}
            >
              <span className={`absolute top-0.5 left-0.5 w-4 h-4 rounded-full bg-white shadow transition-transform ${prefs.enabled ? 'translate-x-5' : ''}`} />
            </button>
          </label>
        )}

        {/* Categories */}
        <div className="mb-4">
          <p className="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-2">Categories</p>
          <div className="grid grid-cols-2 gap-2">
            {CATEGORIES.map(({ key, label, color }) => (
              <label key={key} className="flex items-center gap-2 cursor-pointer">
                <input
                  type="checkbox"
                  checked={prefs.categories.includes(key)}
                  onChange={() => toggleCategory(key)}
                  className="rounded text-primary-600"
                  disabled={!prefs.enabled || permissionStatus !== 'granted'}
                />
                <span className={`w-2 h-2 rounded-full ${color}`} />
                <span className="text-sm text-gray-700">{label}</span>
              </label>
            ))}
          </div>
        </div>

        {/* Timing */}
        <div>
          <p className="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-2">When to notify</p>
          <div className="space-y-2">
            {ADVANCE_OPTIONS.map(({ days, label }) => (
              <label key={days} className="flex items-center gap-2 cursor-pointer">
                <input
                  type="checkbox"
                  checked={prefs.advanceDays.includes(days)}
                  onChange={() => toggleAdvance(days)}
                  className="rounded text-primary-600"
                  disabled={!prefs.enabled || permissionStatus !== 'granted'}
                />
                <span className="text-sm text-gray-700">{label}</span>
              </label>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default NotificationSettings;
```

- [ ] **Step 2: Update Header.js to accept and render bell button**

Add a `onBellClick` prop and render a bell icon button on the right side:

```jsx
import { Bell, Calendar, GraduationCap, MapPin, Phone } from 'lucide-react';

const Header = ({ onBellClick }) => {
  // ...existing code...
  // In the right-side flex group, add before the closing </div>:
  return (
    // existing JSX but add this button at the end of the right-side items:
    <button
      onClick={onBellClick}
      className="p-2 rounded-lg text-gray-500 hover:bg-gray-100 transition-colors"
      title="Notification settings"
    >
      <Bell className="h-5 w-5" />
    </button>
  );
};
```

- [ ] **Step 3: Wire up in App.js**

Add imports:
```js
import NotificationSettings from './components/NotificationSettings';
import { useNotifications } from './hooks/useNotifications';
```

Add after the `events` state:
```js
const { prefs, updatePrefs, permissionStatus, requestPermission } = useNotifications(events);
const [showNotifSettings, setShowNotifSettings] = useState(false);
```

Update `<Header />` call:
```jsx
<Header onBellClick={() => setShowNotifSettings(true)} />
```

Render settings panel (alongside EventModal and SearchModal):
```jsx
{showNotifSettings && (
  <NotificationSettings
    prefs={prefs}
    updatePrefs={updatePrefs}
    permissionStatus={permissionStatus}
    requestPermission={requestPermission}
    onClose={() => setShowNotifSettings(false)}
  />
)}
```

- [ ] **Step 4: Verify build**
```bash
CI=false npm run build 2>&1 | grep -E "^(ERROR|Failed)" | head -5
```
Expected: no output.

- [ ] **Step 5: Final commit**
```bash
git add src/components/NotificationSettings.js src/components/Header.js src/App.js src/hooks/useNotifications.js
git commit -m "feat: browser push notification settings"
```

---

## Task 12: Final Verification & Push

- [ ] **Step 1: Full build check**
```bash
CI=false npm run build 2>&1 | tail -5
```
Expected: `Successfully compiled` with no errors.

- [ ] **Step 2: Run tests**
```bash
CI=true npm test -- --watchAll=false 2>&1 | tail -10
```

- [ ] **Step 3: Final audit check**
```bash
npm audit --audit-level=high 2>&1 | tail -5
```

- [ ] **Step 4: Push**
```bash
git push origin main
```
